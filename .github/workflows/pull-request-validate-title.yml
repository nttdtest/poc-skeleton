name: Validate PR Title

on:
  pull_request:
    types: [opened, reopened, edited, ready_for_review]

jobs:
  validate-title:
    name: Validate PR Title
    permissions:
      contents: read
      pull-requests: write
    uses: launchbynttdata/launch-workflows/.github/workflows/reusable-pr-conventional-commit-title.yml@0.14.2
