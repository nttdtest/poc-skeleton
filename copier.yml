_answers_file: ".copier-answers.yml"
_subdirectory: "template"

_skip_if_exists:
  - NOTICE
  - LICENSE

_exclude:
  - README.md

tasks:
  # Remove the legacy TF workflows if they exist
  - rm -f .github/workflows/increment_tagged_version.yaml
  - rm -f .github/workflows/lint-terraform.yaml
  - rm -f .github/workflows/validate-branch-name.yaml
  # Remove the cloud-specific TF workflows if they exist
  - rm -f .github/workflows/pull-request-terraform-check-aws.yml
  - rm -f .github/workflows/pull-request-terraform-check-azure.yml
